# OPERATIONS.md · 运维备忘（模板 · 配套工作手册 v6）

> 定位：运行期需要知道、但**从界面上看不出来**的行为。每条注出处（决策号 / 任务号）。
> 进入判据：一个新运维在界面上点不出来、在日志里读不懂的行为，才进本文件；界面能自解释的不要写。
> 用法：把 {占位} 换成本项目实际值，删掉不适用的节。

## §1 · 错误码语义与处置动作

> 每个码不只写「什么意思」，必须写「**看到它该做什么**」——错误码的价值在处置动作。

| 码 | 语义 | 处置 |
|---|---|---|
| {429} | {并发闸门被占用，不是系统坏了} | {等当前批跑完再发起，别缩短间隔重试} |
| {504} | {单次请求范围太大} | {缩小范围 / 分批，不是无脑重试} |
| {409} | {配额满 / 前置条件不成立} | {先处置根因（清配额、补前置），重试无意义} |
| {…} | | |

## §2 · 界面上看不出来的行为

- {客户端断开 ≠ 任务取消：已提交的后台任务会跑完（孤儿任务）；用户"关了页面再开"看到的状态从哪来}
- {反代超时会把仍在执行的任务伪装成失败——连锁误判链：用户重试 → 双份任务}
- {定时任务清单：什么时间跑什么、失败了去哪看}

## §3 · 关键 `.env` 配置表

| 变量 | 默认值 | 改动影响 |
|---|---|---|
| {NAME} | {value} | {调大/调小分别发生什么} |

## §4 · 容量与增长

- {磁盘：什么在涨（备份 / 镜像 / 日志 / 表膨胀）、上限是多少、人工清理动作}
- {写放大：哪些操作产生远超直觉的写入（历史表、autovacuum 类背景行为）}

## §5 · 防线层级（哪条防线依赖哪条前提）

> 单独看每条防线都成立，叠加时的前提关系必须写明——{示例：磁盘配额检查只在任务串行时成立 → 需要并发闸门补位；配额是绊线不是墙}。

- {防线 A 成立的前提是 {…}；被 {防线 B} 兜底}
