⟦VC-SELFCHECK-v5.2-BEGIN⟧
# SELFCHECK.md · 大模型自查手册（机读版 · 源自工作手册 v5.2）

## ⟦M0⟧ 执行契约（读到本文件即生效，不可跳过）

1. 本文件是会话启动自查协议。读完后必须先输出【自查报告】（格式见 ⟦M9⟧），再开始任何任务。
2. 防遗忘协议：
   - 本文件有 10 个段标 ⟦M0⟧–⟦M9⟧ 和结尾哨兵 ⟦VC-SELFCHECK-END⟧。报告末尾必须回显实际读到的段标清单；缺任何一个 = 读到的是截断内容，必须重读全文。
   - 输出的每一条判定/建议必须引用规则 ID（如 R1.3、P3.G1）。无 ID 的判定视为臆造，不许输出。
   - 会话中每完成一个任务、或每次给出"可以合并 / 可以进下一步"结论前，重新对照相关规则 ID；若复述不出某个被引用 ID 的内容，说明上下文已丢失，立即重读本文件再继续。
   - 禁止用"一般最佳实践"替代本文件规则；冲突时以本文件与项目 CLAUDE.md 为准，两者再冲突以 CLAUDE.md 为准。

## ⟦M1⟧ 阶段判定（依据仓库状态判断，不依赖用户口述）

按顺序检查，第一个不满足的信号即当前阶段：

| 判定信号 | 当前阶段 |
|---|---|
| CLAUDE.md 不存在或全是 {占位} | P0 初始化 |
| PRD.md 缺失，或核心章节（业务规则/用户旅程/四态/三轨验收）为占位 | P1 需求规划 |
| 产品有前端，但 DESIGN.md 缺失或为占位 | P1.5 设计系统 |
| PLAN.md 无里程碑/任务 | P2 拆任务 |
| PLAN 当前里程碑有未勾任务 | P3 执行中 |
| 任务全勾但收口未完成（无走查记录 / CHANGELOG 无本里程碑条目 / PR 未合并） | P3.5 收口 |
| CHANGELOG 已有 1.0.0 | P4 迭代期 |

辅助取证命令：`git log --oneline -20`、`git status`、`git log origin/main`、`gh pr list`、`gh pr checks`。
判定与用户口述不一致时：报告差异与证据，等用户裁决，不擅自跳阶段（见 R6.3）。

## ⟦M2⟧ 硬规则断言 R1.*（任何阶段、任何档位，违反即停）

- R1.1 密钥只在 `.env`（已 gitignore），仓库只有 `.env.example` 占位。
- R1.2 禁直 push main；一律分支 → PR → 合并。
- R1.3 合并前 CI 全绿；建议合并前必须先贴 `gh pr checks` 结果；红 = 先修，不建议合并。
- R1.4 契约单一真相在 shared 包（zod/schema 定稿 → 推导类型）；PRD 只放指针；禁止两处定义。
- R1.5 面向用户文案走 i18n key（zh/en 双份），禁硬编码。
- R1.6 迁移经工具生成，不裸写 SQL，不删改迁移历史。
- R1.7 危险命令（`rm -rf` / `DROP TABLE` / `git push --force` / `git reset --hard`）执行前停下确认（常规 node_modules/dist 清理除外）；优先 permissions 硬拦截。
- R1.8 新依赖先说明理由、等确认。
- R1.9 涉敏里程碑（鉴权/支付/PII/文件上传/用户输入入库）收口必过 A5 安全审计（子代理或新会话跑，禁止实现会话自审），高危清零才能合并；CI 必含 secret 扫描 + 依赖审计。

## ⟦M3⟧ 工作方式断言 R2.*

- R2.1 一会话一任务（或一批【可批量】）；做完 commit + 勾 PLAN + /clear。
- R2.2 commit 粒度：代码 + PLAN 勾选 + DECISIONS 追加（如有）同一 commit。
- R2.3 命门【单独+确认】绝不混进批量。
- R2.4 只做本任务，不顺手改无关模块、不提前做下一任务。
- R2.5 PLAN/PRD/DECISIONS 只增量编辑，禁整文件重生成；归档只移不删；改完 `git diff` 核对 checkbox 数量与结构。
- R2.6 前端视觉唯一依据 DESIGN.md；组件内禁硬编码颜色/间距/字号。
- R2.7 双轨并行：切片契约冻结后前后端两泳道可并行；后端先行上限 = 本切片 + 下一切片【设计无关】任务；绝不预做未来旅程的 API；里程碑完成 = 旅程走通；收口前必有"按旅程联调"任务。
- R2.8 文档规模化：PLAN 只留当前里程碑（收口时归档进 PLAN-ARCHIVE）；DECISIONS 活跃卷只留索引 + 当前 Phase，满 Phase 归档 DECISIONS-ARCHIVE；子系统参考文档（{DOMAIN}.md）头部声明"代码为真相源"+ 最后同步戳，收口时同步；Phase 设计文档必须提交入库。

## ⟦M4⟧ 分档判定 R3.*

- R3.1 三问：出错代价大？逻辑复杂易错？不可逆（动数据/钱/安全）？任一"是" = 重型。
- R3.2 命门清单：鉴权 / 实时同步 / 状态机 / 数据模型 / 核心算法 / 动既有核心时序 / 动钱（支付/账本/发放）/ 外部 webhook 入口 → 重型 +【单独+确认】；动钱与 webhook 另过 A8 支付集成检查单。
- R3.3 档位配置：重型 = Opus + Plan 模式 + ultrathink + 逐步陪核（模板 3A）；常规 = Sonnet、不 ultrathink、做完看结果（3B）；轻型 = 直接做，不套模板；批量 = 3C，一任务一 commit，命门不入批。
- R3.4 警报：所有任务按重型跑 = token 浪费；轻型跳过 R1.* = 违规。分档只调流程重量，不调安全标准。

## ⟦M5⟧ 阶段门禁（Px.G = 出场条件；Px.F = 常见缺陷 → 补救）

- P0.G1 出场：CLAUDE.md 定稿（TIER1/TIER2/易错点）+ 机制层就位（`.claude/settings.json` permissions、pre-commit 密钥扫描、CI secret 扫描 + 依赖审计）。
- P0.F1 机制层缺失、只有文档纪律 → 补配置；属 M0 DoD 欠账，优先级[必须]。
- P1.G1 出场：PRD 含 业务规则 / 数据契约指针 / 用户旅程（无断头路）/ 页面清单 / 四态表（含错误码→错误态映射）/ 设计方向 / 非功能（安全/性能/可观测性/数据备份）/ 三轨验收 / 测试策略 / 成功指标。
- P1.F1 只有功能列表、无旅程与四态 → 两张皮前兆；补齐后才准拆任务。
- P1.5.G1 出场：DESIGN.md 定稿 + CLAUDE.md 已回填"改样式先读 DESIGN.md"。
- P2.G1 出场：PLAN 里程碑 = 垂直切片（判据：一个新用户能用它完成什么并看到结果）；每任务带【复杂度】【执行模式】（全栈加泳道/【设计无关】）；每里程碑含收口验收门任务；涉运营数值的里程碑 T0 = 数值拍板门【单独+确认】；DECISIONS 骨架建好。
- P2.F1 里程碑按技术层切（如"后端里程碑"）→ 重切为竖切；纯地基仅允许 M0。
- P3.G1 每任务出场：完成定义达成 + 测试 lint 绿 + commit + PLAN 勾选；前端任务另含四态/断点/DESIGN 对照（屏级 B2 已做、commit 前过 B3）+ 布局程序化断言（scrollWidth-clientWidth===0、最右元素不超容器右缘、375px 重跑——四命令全绿抓不到裁切溢出）。
- P3.F1 前端只验"渲染出来" → 补四态/断点/DESIGN 对照后才算完成。
- P3.F2 命门连跑未逐个核验 → 合并前对最危险 1–2 个做针对性自审（只读、证据 file:line、扫竞态/降级/信任边界）。
- P3.5.G1 里程碑出场：旅程走查无断路 +（涉敏）A5 零高危 + 四命令绿 + `gh pr checks` 绿 + PR 合并 + CHANGELOG 记 `0.{n}.0` + 本里程碑任务归档进 PLAN-ARCHIVE + 已合并分支删除（本地+远程）+（有子系统文档）`{DOMAIN}.md` 已同步。
- P3.5.F1 有断路缺口 → 必须补完才准进下一里程碑；信息缺失/打磨类记 DECISIONS 或下一里程碑。
- P3.5.F2 上一里程碑有 commit 未进 main（`git log origin/main` 核对）→ 先补 PR 再切新分支；`gh run list --branch main` 确认 main 自身 CI 为 success，红着不准起跑。
- P4 流水线：4.0 定性（bug↔需求变更，定文档流向）→ 4.1 变更单（复述确认/边界/验收反例）→ 4.2 半径分级（微调/常规/大改动）→ 4.3 影响面分析（模块/契约/数据/测试/旅程/文档）→ 执行（微调直做可攒批 | 常规两步：方案确认→分步改 | 大改动 = 迷你 0→1：改 PRD → 迭代里程碑 → P3 循环）→ 4.6 收尾（版本号跟半径走 + 代码文档同批 commit + 大改动回归走查）。
- P4.F1 大改动没走 4.3 影响面分析直接改 → 停下补做，再继续。
- P4.F2 接支付/webhook/账本功能未过 A8 检查单（验签+幂等+回传实证 / fake 双轨 / 测试配置封闭 / 实付 E2E / 退市兼容 / 账本对账）→ 停，逐项补齐。
- P4.G2 Phase 模式：子系统连做 ≥2 里程碑 → 升格 Phase：开工规划拆解（设计文档入库 + 拍板门 + DECISIONS）→ 各里程碑照常循环 → Phase 总收口（跨里程碑 E2E + 子系统文档同步 + PRD 骨架修订 + PLAN/DECISIONS 归档）。
- P5 评审规则：跨模型评审只输出报告 + 逐段修改建议；由原模型增量回填；禁整文件重写。

## ⟦M6⟧ 审计协议 R5.*（被要求"审核项目"时执行）

- R5.1 文档层：七文件在位且各司其职；PRD = 当前真相不堆历史；PLAN 只装未做；DECISIONS/CHANGELOG 只增不改；契约唯一真相在 shared 包。
- R5.2 git 层：一任务一 commit；无直推 main；无带红合并的 PR；上一里程碑全部 commit 已进 main。
- R5.3 门禁层：每个已收口里程碑有 走查记录 / CHANGELOG 条目 / CI 绿证据；涉敏里程碑有 A5 留痕。
- R5.4 机制层：permissions / pre-commit / CI 安全步骤实际配置，而非只写在文档里。
- R5.5 产品层：提示用户亲自走旅程；此层不可由 AI 代替。
- 报告排序：违规（必须整改）> 缺失留痕（补记）> 建议；每条带证据（文件:行 / commit hash / PR 链接）+ 规则 ID。

## ⟦M7⟧ 补救速查（诊断 → 处方）

| 诊断 | 处方 |
|---|---|
| PRD 顶部堆版本历史 | 增量剪切到 CHANGELOG，PRD 恢复当前真相（R2.5） |
| 决策日志混在 PLAN | 逐条剪切到 DECISIONS，禁整文件重写（R2.5） |
| CI 红被合并进 main | 立即开修复分支清红；清红前禁止新功能合并（R1.3） |
| 契约在 PRD 与代码两处定义 | 以 shared 包为准，PRD 改指针（R1.4） |
| 本地绿 CI 红 | 本地模拟 CI：清 dist/缓存 + 无 .env，按 CI 步骤顺序跑 |
| PLAN checkbox 丢失/结构损坏 | 从 git 历史恢复；后续只增量编辑（R2.5） |
| 连续 3 次修复失败 | 停止尝试，进入纪律调试：未查明根因不许改；3 次后质疑架构 |
| 出现"先不做/以后再做"决策 | DECISIONS 记伏笔四要素（延后什么/归属/占位/为何）+ PLAN 交叉引用 |
| 后端空闲、前端定义未就绪 | 按 R2.7 空档排序：①下一切片【设计无关】核心 ②质量债（A3/测试/性能基线）③线框占位 UI 先走通旅程；禁止预写未来旅程 API |
| PLAN 内已完成里程碑 >1 个未归档 | 收口欠账：整段剪切移入 PLAN-ARCHIVE，PLAN 留一行（R2.8） |
| DECISIONS 膨胀（>1 个 Phase 未归档 / 读不动） | 建索引表 + 满 Phase 条目整卷移入 DECISIONS-ARCHIVE（R2.8） |
| 子系统规则散落、PRD 停在 MVP 失真 | 开 `{DOMAIN}.md` 参考文档（代码为真相源 + 出处 + 同步戳）；Phase 收口时修订 PRD 骨架层（R2.8） |
| UI 改动四命令全绿但界面破 | 补布局程序化断言：溢出/裁切/对齐/375px（P3.G1） |
| 涉支付/webhook/账本 | 逐项过 A8 检查单；数值先过拍板门（P4.F2、R3.2） |
| 机制层为零的存量项目 | 立一次性补装任务：permissions deny + gitleaks（pre-commit+CI）+ 依赖审计 + `.claude/commands` + SELFCHECK 接入（R5.4） |

## ⟦M8⟧ 建议生成规则 R6.*

- R6.1 建议必须落到具体对象：下一个任务 / 要补的门禁 / 要改的文档段落；禁止"加强测试"式空话。
- R6.2 每条建议标 [必须|应该|可选] + 规则 ID + 建议档位（重型/常规/轻型）。
- R6.3 阶段判定与用户口述冲突 → 呈现差异与证据，等裁决；不擅自跳阶段、不擅自补做。
- R6.4 发现超出本次任务范围的问题 → 记录并建议，不顺手修（R2.4）。

## ⟦M9⟧ 自查报告格式（会话开场强制输出，缺项无效）

```
[VC-SELFCHECK v5.2 已加载]
1 阶段判定：P{x}（证据：…）
2 硬规则体检：R1.1–R1.9 逐条 通过/违规/无法判定（违规附证据与处方）
3 阶段门禁：P{x}.G 状态；发现的 P*.F 缺陷 → ⟦M7⟧ 处方
4 待办与建议：[必须/应该/可选] + 规则 ID + 档位
5 本会话计划：任务 = {T-编号}；档位 = 重型/常规/轻型；模板 = 3A/3B/3C；涉敏 = 是/否
CHECK-ECHO: M0 M1 M2 M3 M4 M5 M6 M7 M8 M9 | END-SEEN=yes | 引用规则数={n}
```

⟦VC-SELFCHECK-END⟧
