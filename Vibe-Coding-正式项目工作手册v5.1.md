# 正式项目版 Vibe Coding 工作手册

> 在原"一套提示词覆盖大部分 Vibe Coding 场景"基础上改造，
> 保留：角色提示、版本规划、MVP/后续版本拆分、Business Rules + Data Contract、跨模型评审。
> 补强：持久规则层(CLAUDE.md)、git 回滚点、按模块拆会话 + /clear、验收标准与测试、可控的修复循环、活规格与变更日志分离。
> v5 修订（结构性）：**里程碑从"技术层"改为"用户可玩的垂直切片"**——每个里程碑交付一条"用户点击→看到结果"的完整竖切，而非"先做完所有后端再做前端"。配套：三轨验收（功能/体验/旅程）、每个里程碑末尾强制"用户旅程走查"、把 partygames 实战踩过的坑固化成 CLAUDE.md 硬规则（CI 绿才合并、尾巴漏合、本地≠CI、PLAN 护栏、接口隔离、批量飞命门慢、合并前自审、伏笔管理）。这才是终结"功能做完、产品像 demo"两张皮的根。
>
> v5.1 增补：**阶段 4 扩写为"上线后迭代分级流水线"**——定性（bug↔需求变更）→ 变更单（准确理解需求）→ 改动半径分级（微调/常规/大改动）→ 影响面分析（跨模块改动的护栏）→ 按半径执行 → 回归走查收口。大改动 = 迷你 0→1，复用垂直切片/三轨/护栏全套机制。**新增护栏 9 双轨并行**：切片内契约冻结后前后端两泳道并行，后端先行有上限，里程碑完成仍以旅程走通为准。
>
> v5.1 二次增补（partyGames 运营期 M8–M31 复盘）：文档规模化护栏（PLAN/DECISIONS 归档节拍，见文档分工）、Phase 模式（4.7）、子系统参考文档（`{DOMAIN}.md`）、命门清单扩充（动钱/webhook）与支付集成检查单（A8）、PRD 两层结构（骨架层 + 里程碑细节 JIT）、UI 布局程序化断言（B3）、拍板门（阶段 2）、存量项目机制层补装（A7）。
>
> 配套文件：`CLAUDE.md`(项目宪法) · `PRD.md`(活的需求规格) · `PLAN.md`(任务接力棒) · `DECISIONS.md`(决策日志) · `CHANGELOG.md`(版本日志) · `DESIGN.md`(设计系统，阶段 1.5 产出)

---

## 文档分工(先理解，再用提示词)

原套路把所有赌注押在一个 PRD.md 上、并往它顶部不断堆版本更新——项目一大就臃肿、烧上下文、被旧内容误导。正式项目里拆成四个文件，各司其职：

| 文件 | 作用 | 变化频率 | 谁写 |
|------|------|---------|------|
| `CLAUDE.md` | 怎么做事：技术栈约定、命名规范、禁止项、易错点 | 稳定，规则变才改 | 你 + AI 协助 |
| `PRD.md` | 做什么：当前版本的需求规格(活文档，直接改而非堆叠) | 按版本演进 | AI 生成、你审 |
| `PLAN.md` | 任务清单 + 完成的定义(接力棒) | 每个任务后更新 | AI + 你 |
| `DECISIONS.md` | 决策日志：关键架构/取舍决策(只增不改) | 有决策时追加 | AI 追加、你审 |
| `CHANGELOG.md` | 版本变更历史(只增不改) | 每个版本追加 | AI 追加 |
| `DESIGN.md` | 前端视觉的单一真相：设计系统(活文档) | 设计语言演进才改 | AI 生成、你审 |
| `{DOMAIN}.md` | 子系统参考文档(按需)：代码为真相源的同步镜像 | 里程碑收口时同步 | AI 生成、你审 |

**关键原则**：活规格(PRD)直接修改保持"当前真相"；历史变更进 CHANGELOG。两者分开，PRD 永远干净。同理，**决策日志从一开始就独立成 `DECISIONS.md`，不要留在 PLAN 里**——决策只增不改、会越积越长，留在 PLAN 里会让"每次读 PLAN 都拖入几千 token 的历史"，PLAN 只装"还要做什么"，DECISIONS 装"为什么这么定"。

**版本号 ↔ 里程碑的映射（MVP 期就开始记 CHANGELOG，别等上线）**：每合并一个里程碑进 main，CHANGELOG 追加一条 `0.{n}.0`（M0=0.1.0、M1=0.2.0…），一句话写清"新用户现在能完成什么"；正式上线 = `1.0.0`；上线后版本号跟改动半径走（阶段 4.6）。CHANGELOG 走**轻量模式**：条目由收口验收门顺手写、最少一行，不为版本号单独开决策——真实项目的实际结局是"里程碑编号就是版本"，制度别比现实重。

**文档规模化护栏（长寿项目必然遇到，从第一天按节拍执行）**：① **PLAN 只留当前里程碑**——每次收口把本里程碑任务清单移入 PLAN-ARCHIVE，动作写死在收口验收门任务里，不靠自觉；② **DECISIONS 分卷**——活跃文件只留「索引（D# | 日期 | 一句话 | 生效/被D#取代）+ 当前 Phase 条目」，满一个 Phase 整卷移入 `DECISIONS-ARCHIVE.md`；③ 子系统另开参考文档，别塞进 PRD。实证：不设节拍的真实项目跑到 M31 时，DECISIONS 膨胀到 381KB、PLAN 229KB、28 个已完成里程碑从未归档——"只增不改"没有配套归档策略，就是给每个会话埋的上下文炸弹。

**子系统参考文档（`{DOMAIN}.md`，如 ECONOMY.md）**：数值密集型子系统（经济/数值平衡/规则表）的单一真相在**代码常量**里，文档反向作"同步镜像"——与"PRD=真相、代码服从文档"方向相反，但同样合法。开档条件：子系统规则跨 ≥2 个里程碑。硬要求：头部声明"本文是从代码读出来的参考，不是真相源"、每条规则标注代码出处、带「最后同步」时间戳；同步时机固定在里程碑收口门。Phase 级设计文档（如 `XX-V3-DESIGN.md`）必须提交入库，禁止游离在 git 之外。

---

## 核心原则 · 垂直切片：里程碑是"用户能完成的事"，不是"一块技术"

> 这一节是 v5 的地基，也是终结"功能做完、产品像 demo"两张皮的根本。放在最前面，因为它决定后面每个阶段怎么拆、怎么验。

### 两张皮从哪来

按"技术层"推进（先做完引擎、再做账户、再做实时、再做匹配……），每一块技术单独都对、都有测试，但**没有任何一个里程碑验的是"一个真实用户能从头到尾完成一件他想做的事"**。于是攒到最后一刻，你会发现：功能都在，但页面之间是断的、用户不知道下一步、玩完不知道为什么再来——一堆正确的零件，不是一个能用的产品。这不是某个页面没做好，是**推进单位错了**。

打个比方：按器官造人（先造完所有骨骼、再造所有血管、再铺皮肤），中间那个"人"永远站不起来、走不了路；只有到最后一步才可能是活的。给每个器官加质检表也救不了——顺序本身就是病根。

### 解法：里程碑之间按垂直切片切，分层只用于切片内部

**垂直切片 = 一条从"用户点击"到"用户看到结果"贯穿所有层（前端→API→逻辑→数据库）的最小完整功能。** 哪怕很薄，它必须是"用户真的能走通的一件事"。

- **里程碑之间**：按"用户能完成的动作"切。例：M1 不是"纯引擎"，而是"**一个访客能打开网页、和 AI 下一局、看到输赢**"——这条竖切里，前端、后端、引擎各做最小的一块，但拼起来用户能玩。
- **切片内部**：才用 `数据库→算法→API 契约→前端` 分层顺序推进（这个顺序对，用来组织"一条竖切内部怎么做"）。

**判据**：每个里程碑结束时，问一句——"一个什么都不知道的新用户，现在能不能真的用它完成点什么、并看到结果？" 答不上来"能"，这个里程碑的切法就还是技术层、不是垂直切片。

### 为什么这样能根治两张皮

因为**每个里程碑结束都有一个真实用户能用的东西**，脱节在 M1、M2 就会暴露（你走一遍就发现断头路），而不是攒到 M7 才集中爆发。产品的连贯性从第一个里程碑起就被反复验证，不可能"做完才发现是 demo"。

> 例外：纯地基（如 M0 工程脚手架/CI/仓库初始化）没有用户可见面，允许作为"第 0 个非切片里程碑"。从第一个有用户价值的里程碑起，一律垂直切片。

---

## 任务分档 · 按复杂度决定用多重的流程（省 token 的核心）

> 这套流程是**工具箱，不是必经仪式**。完整流程（Plan + ultrathink + 测试前置 + 评审）是为"复杂、易错、不可逆"的功能设计的；常规功能用全套就是浪费 token。
> **拿到一个任务，先归档，再决定用多重的流程。** 这是控制成本最有效的一招——不分档、所有任务都按最高规格跑，配额会很快烧光。

### 归档口诀（10 秒判断）

问三个问题：**① 出错代价大吗？② 逻辑复杂/易写错吗？③ 不可逆吗（动数据、动钱、动安全）？**
- 三个都"否" → **轻型**或**常规**
- 任意一个"是" → **重型**

### 三档配置

| 维度 | 重型 Heavy | 常规 Standard | 轻型 Light |
|------|-----------|--------------|-----------|
| 典型场景 | 算法、实时同步、鉴权安全、数据模型、架构决策、复杂调试、**设计系统/信息架构决策**、**动钱（支付/账本/发放）**、**外部 webhook 入口** | CRUD、表单、列表页、普通 API 端点、**套用既有设计系统铺页面** | 改文案、调样式、加字段、配置微调 |
| 模型 | Opus | Sonnet | Sonnet / Haiku |
| ultrathink | ✅ 是 | ❌ 否 | ❌ 否 |
| Plan 模式 | ✅ 必须，等你确认 | 看情况（一句话说不清才 Plan） | ❌ 直接做 |
| 测试前置 | ✅ 反例作硬指标 | 关键路径有测试即可 | 一般不需要 |
| 评审（附录 A/B） | ✅ A2 工程评审 / A3 偏执 / A5 安全 按需 | 选用 | ❌ |
| 你的核验 | 逐步看 diff、陪核关键步 | 做完看结果 | 扫一眼 |
| /clear | 任务间必 clear | 任务间必 clear | 可连着做几个小改再 clear |
| commit | 同提交（代码+PLAN+DECISIONS） | 同提交 | 可批量 |

### 不随档位降低的底线（重要）

分档调的是"流程的重量"，**不是"安全的标准"**。CLAUDE.md 的 TIER 1 硬规则对**所有档位**一律生效，轻型任务也不例外：禁提交密钥、禁直接 push main、契约先行、禁硬编码文案、迁移不裸 SQL。**轻型 ≠ 可以乱来，只是少一些"想清楚再写"的仪式，不是少一些纪律。**

### 省 token 的四个立刻可做的动作

1. **常规/轻型任务去掉 `ultrathink`**——这是最大的一笔。ultrathink 只留给算法/架构/复杂调试/评审。
2. **简单任务跳过"出计划等确认"那一轮**——一句话能说清的，直接让它做、做完你看结果。
3. **决策日志独立成 `DECISIONS.md`**——从一开始就和 PLAN 分开（见文档分工），避免每次读 PLAN 都拖入几千 token 的历史。若旧项目的决策日志还混在 PLAN 里，拆分时**只增量移动、逐条剪切，别整文件重写**（整文件重写极易丢任务或抹掉 checkbox）。
4. **按档位分级用模型**——别全程 Opus；常规用 Sonnet，琐碎用 Haiku，把 Opus 留给重型。

### 落到本项目的示例（M2 为例）

同一个里程碑内部，不同任务用不同档：
- AI 思考时间控制、引擎接 HTTP 的契约设计 → **重型**（Opus + Plan + ultrathink）
- `ai-move` 端点的 CRUD 部分、前端棋盘渲染 → **常规**（Sonnet，不 ultrathink，做完看结果）
- 铺静态页、调样式、加占位文案 → **轻型**（Sonnet/Haiku，直接做）

> 一句话：**核心算法那种"该费"的继续费（那是买保险）；其余的轻装上阵。** 费得值不值，取决于你有没有按复杂度分档。

---

## 阶段 0 · 项目初始化(原套路缺这一步)

新项目第一件事，不是写需求，是立规则。在 Plan 模式下输入：

```
你将帮我初始化一个项目的 CLAUDE.md（项目宪法）。先通过提问了解：
1. 项目定位与一句话目标
2. 技术栈、运行/部署环境
3. 已有的代码规范、命名约定、目录结构（若是全新项目，给出你的推荐供我确认）
4. 绝对禁止项（如：禁止提交密钥、禁止直接 push main、禁止引入未经我确认的新依赖）

了解清楚后，生成一份 200 行以内的 CLAUDE.md，把"硬规则"放最前面。
先不要写任何业务代码。ultrathink
```

> Ultrathink 是 Claude Code 里让模型尽力思考的参数，其他 IDE 可删。

> **初始化时顺手把"机制层"立起来（细节见附录 A7）**：`.claude/settings.json` 里配危险命令 permissions（deny/ask）、pre-commit 密钥扫描（gitleaks 类）、CI 里加 secret 扫描 + 依赖审计（npm audit 类）、把附录的高频评审角色做成 `.claude/commands/` 自定义命令。这些属于 M0 的 DoD，不是"以后再说的优化"——能用机制硬拦的规则，不要只写在 CLAUDE.md 里靠嘴。M0 的 DoD 还包括一份能跑起来的 README（安装 / 启动 / 测试三段）——真实项目跑到 300+ commit 时 README 还是空的，新环境每次都靠考古。

---

## 阶段 1 · 0→1 需求规划(修订原 0→1 提示词)

**改动说明**：删掉了原版混进来的"随时查看代码库 / 本次修改影响哪些现有文件"——这些是迭代场景的指令，全新项目里没有代码库，无意义。新增：验收标准、测试策略、非功能与安全需求作为 PRD 一等公民（原套路只能靠事后评审补这些）。

**PRD 采用两层结构（长项目防失真的关键）**：阶段 1 只产出**骨架层**——业务规则、契约签名级骨架、旅程、三轨标准，这些稳定少改；每个里程碑的字段级规格留到**该里程碑开工前**，在 PRD"里程碑细节"一节（模板 §11）JIT 补齐。一次性写全量规格，在跑过十几个里程碑的真实项目里必然失真；渐进细化才可维护。

在 Plan 模式下输入：

```
现在，你将扮演一名首席产品设计师 + 解决方案架构师，既有顶级产品的设计审美与战略思维，
也对工程可行性、安全与数据有敏锐判断。我们的目标是共同规划一款能持续迭代的产品，
先从一个成功的最小可行产品（MVP）开始。

你的任务：
1. 启发式对话：我会描述产品愿景（可能附上草图、线框图或竞品截图，请把它们纳入讨论），你逐一挖掘并质询所有模糊的功能细节（一次问一组关键问题）。
2. 设计顾问：主动从用户体验和审美角度提出 UI/UX 建议。
3. 版本规划师：主动区分哪些功能是 MVP 绝对核心、哪些可放后续版本。
   例如提问："这个功能很棒，但为尽快验证核心价值，是否先做简化版，完整版放 V2？"
4. 当你认为一切清晰后，以下面格式输出一份"产品路线图"，作为我们合作的蓝图：

   - 核心目标 (Mission)：一句话描述最终愿景。
   - 用户画像 (Persona)：为谁设计？核心痛点是什么？
   - V1 / MVP：列表形式，列出第一版必须包含的核心功能。
   - V2+ (Future Releases)：列表形式，未来版本计划。
   - 关键业务逻辑 (Business Rules)：MVP 的核心业务规则。
   - 数据契约 (Data Contract)：MVP 需要处理的数据结构。
   - 【产品体验·必填】用户旅程 (User Journey)：把每条业务规则翻译成"用户从哪进入→走哪几步→每步看到什么反馈"的旅程；用 Mermaid 画关键流程，确保每条规则都有对应的用户可见路径，没有断头路。
   - 【产品体验·必填】页面/路由清单 (Screen Inventory)：列出 MVP 的每一屏/路由，标注每屏的主/次/三级信息层级。
   - 【产品体验·必填】关键交互状态 (Interaction States)：逐个核心功能定义 加载/空/错误/成功 四态，每态直接挂回触发它的业务规则与数据契约字段。
   - 【产品体验·必填】设计方向 (Design Direction)：产品要传达什么（信任/速度/质感/温度…），作为阶段 1.5 建 DESIGN.md 的输入。
   - 【新增】非功能需求：安全（鉴权/权限/数据写入边界）、性能、隐私、可访问性等约束。
   - 【新增】验收标准 (Acceptance Criteria)：三轨制——① 功能轨：每个核心功能"算完成"的可验证标准；② 体验轨（单页）：交互四态齐全、响应式断点、可访问性、对照 DESIGN.md 一致性；③ 旅程轨（跨页）：整条用户旅程连贯、无断头路、每步有明确下一步、结束态有结果反馈。功能跑通但体验轨/旅程轨未达标，不算完成。（体验轨管"单个页面完整"，旅程轨管"页面之间连贯"——两张皮往往死在旅程轨，别只顾单页。）
   - 【新增】测试策略：关键路径如何验证（单测/手动验收清单）；手动验收清单须覆盖交互四态与移动端断点，不只是 happy path。

5. 架构设计蓝图：基于上面内容，给出：
   - 核心流程图：用 Mermaid 的 sequenceDiagram 或 flowchart 画关键业务/数据流。
   - 技术选型与风险：说明关键选型（框架/库/算法）并预判技术风险。

6. 最终确认与存档：在我确认路线图后，将以上全部内容生成 PRD.md 存档，然后等待我的下一步命令。

关键约束：在我明确说"开始开发"之前，不要写任何业务代码。ultrathink
```

> UI 这一步若想要真实可点的原型而非 ASCII 草图，可用 Claude Design 出原型，再 handoff 回 Claude Code——比 ASCII 高保真一个量级。

---

## 阶段 1.5 · 设计系统(写前端代码前先立 DESIGN.md，别等到附录)

**为什么提到主流程**：DESIGN.md 必须在拆任务和写前端**之前**就存在——阶段 0 的 CLAUDE.md 要引用它、阶段 2 拆出的前端任务要对照它验收。把它留在附录 B 当"可选插件"，会导致每个页面风格漂移、一眼 AI 味，产品体验和功能彻底脱节。所以它是主流程的一环，不是附录。

时序：阶段 1 出 PRD（含设计方向）→ **阶段 1.5 建 DESIGN.md 并回填 CLAUDE.md** → 阶段 2 拆任务。

**设计输入（可选，但强烈建议——这是摆脱"AI 味"最有效的一招）** —— 你手上任何现成材料都能喂给它，让 DESIGN.md 长在真实素材上、而不是凭空生成：
- **手绘草图 / 线框图 / 竞品截图** → 直接把图拖进对话，Claude 能读图。
- **参考前端代码 / 现成 `style.css` 原型 / 你喜欢的某个组件** → 贴代码或给文件路径，让它提炼出配色/间距/字体的规律，而不是另起一套。
- **品牌素材** → logo、主题色、指定字体、纹理/背景贴图（如 `dust.webp` 这类）。

> 建议把这些素材统一放 `docs/`（如 `docs/DESIGN-INPUT.md` 串起说明 + 草图图片 + 原型 `style.css` + 贴图），DESIGN.md 从它们倒推，并在 DESIGN.md / CLAUDE.md 注明素材出处。这正是本项目已在用的做法。

**怎么建** —— 在对话里输入（与附录 B1 同源，这里直接给，不用去翻）：

```
扮演资深设计师，和我一起从零搭这个产品的设计系统。

输入有两类：
- PRD.md 里的「设计方向」（产品要传达什么：信任/速度/质感/温度…）；若我没写清，先反问补齐。
- 我附上的设计输入（草图/线框图/竞品截图/参考前端代码/现成 style.css 原型/品牌素材，
  见 docs/ 或下方附件）。**优先从这些真实素材里提炼配色/间距/字体/布局规律，
  而不是凭空生成一套**；与素材冲突处先标出来问我，不要自作主张替换。

倒推出具体选择，逐项给出并都附理由：
- 审美方向：一句话定调，说明它如何服务于上面的设计方向。
- 字体：3+ 款，标明各自角色（标题/正文/数字或代码…）。
- 配色：主色/辅色/中性/语义色（成功/警告/错误），每个带 hex；说明对比度是否满足可访问性。
- 间距刻度：一套基准尺度（如 4/8/12/16…），后续所有间距从中取值。
- 布局策略：栅格/容器宽度/响应式断点。
- 动效策略：什么时候用动效、时长与缓动的基调。

并明确区分：哪些是"符合品类预期的安全选择"，哪些是"让我们有辨识度的刻意冒险"。

定稿后：
1. 把它写成 DESIGN.md 放仓库根目录（这是前端视觉的单一真相）。
2. 在 CLAUDE.md 里加一段：前端视觉以 DESIGN.md 为准，改样式前先读它、
   不准在组件里硬编码颜色/间距/字号——让以后每个会话都遵守这套系统。

先不要写任何前端代码。ultrathink
```

> 设计重的产品：这一步可改用 Claude Design 出可点高保真原型，再 handoff 回 Claude Code，比纯文字描述高一个量级；产出依然落到 DESIGN.md。

**为什么要回填 CLAUDE.md** —— 回填不是目的，是**强制执行的钩子**：会话 `/clear` 后记忆清零，只有 CLAUDE.md 会被自动读进每个新会话。所以在 CLAUDE.md 加一句"前端视觉的单一真相是 DESIGN.md，改样式前先读它、不准硬编码颜色/间距/字号"，才能保证每个会话都会去翻它——否则模型根本不知道这文件存在。

**建立之后谁来读它（闭环，别让它变成写完就扔的产物）** —— DESIGN.md 是"前端视觉的单一真相"，在下游被反复消费：
- 阶段 1 验收·体验轨 与 阶段 2 前端任务的「完成定义」里，"对照 DESIGN.md 一致性"是算完成的硬指标。
- 阶段 3 前端任务循环里，**附录 B2**（计划期评审第 5 项）写代码前对照它、**附录 B3**（实现后视觉审计）跑起来后对照它的偏差并修复。

**它是活文档** —— 和 PRD 一样：设计语言演进（如 V2 加新模块）时直接改 DESIGN.md 保持"当前真相"，不堆历史版本。但每次改动要在 CHANGELOG 的"技术要点"里记一句（改了什么基调、为什么）——否则 B3 视觉审计对着漂移过的 DESIGN.md，无从判断偏差是代码的错还是规范自己变了。

---

## 阶段 2 · 拆任务(原套路缺这一步)

PRD 确认后，把它变成可执行的接力棒。在 Plan 模式下输入：

```
基于已确认的 PRD.md，生成一份 PLAN.md 和一份 DECISIONS.md：
- PLAN.md：把 MVP 拆成有序的里程碑，每个里程碑再拆成带 checkbox 的任务。
- 【垂直切片·最重要】里程碑之间按"用户能完成的完整动作"切，不按技术层切：每个里程碑必须交付一条从"用户点击→看到结果"贯穿前端/API/逻辑/数据的最小完整竖切，哪怕功能很薄，也要是"真实用户能走通的一件事"。纯地基（脚手架/CI）可作为第 0 个非切片里程碑，其余一律垂直切片。
- 【切片内部才分层】在一个里程碑（一条竖切）内部，按 数据库 schema → 算法/核心逻辑 → 后端 API（先把路径/入参/出参/错误码契约定稿）→ 前端（按契约对接）的顺序排任务，用契约把层解耦。分层是"切片内部的顺序"，不是"里程碑之间的切法"。
- 每个里程碑写明【里程碑级完成定义】：一个新用户能用它完成什么、看到什么结果（这就是它的"可玩验收"）。
- 颗粒度：每个任务约 30 分钟能完成、改动文件不要太多；太大的拆成子任务。
- 每个任务写明"完成的定义"（对应 PRD 的验收标准）。**前端任务的"完成定义"必须显式列出交互四态（加载/空/错误/成功）、响应式断点、对照 DESIGN.md 一致性、布局程序化断言（见 B3：无横向溢出/裁切、375px 重跑）——不是"功能渲染出来"就算完。**
- 每个任务标注【复杂度：重型/常规/轻型】和【执行模式：可批量/单独+确认】。分档口诀：出错代价大/逻辑复杂/不可逆，任一为"是"即重型；鉴权/实时同步/状态机/数据模型/核心算法/动钱（支付/账本/发放）/外部 webhook 入口是命门，一律【单独+确认】。
- 全栈切片的任务加泳道标注【后端轨/前端轨】；纯逻辑/算法/已确认实体 schema 类任务可标【设计无关】（允许跨切片前置，见护栏 9）。契约冻结任务是两泳道的共同依赖；收口验收门之前安排一个"按旅程联调"任务（错误码→错误态、加载时序、边界值）。
- 涉及运营数值/业务口径的里程碑（定价、奖励、权益、概率），T0 固定为"数值拍板门"任务【单独+确认】：先锁定数值与口径、记入 DECISIONS，再开发——技术契约冻结管接口形状，拍板门管业务数值，两道闸缺一不可。
- **前端设计评审以"屏/路由"为单位**：每一屏在首个涉及它的前端任务实现前走一次附录 B2 补全交互态/响应式/AI-slop 盲区，该屏后续任务共享结论，不必每个任务重跑；轻型的文案/样式微调豁免。
- 标注任务间依赖关系，并给出"执行批次建议"：哪些任务可批量在一个会话连做、哪些命门必须单独做。
- 每个里程碑的最后一个任务固定为"收口验收门"：端到端实证 DoD + 旅程走查（阶段 3.5）+（涉敏里程碑）A5 安全审计 + CI 绿 + 开 PR + CHANGELOG 追加 `0.{n}.0` 条目 + 本里程碑任务清单归档至 PLAN-ARCHIVE + 删除已合并分支（本地+远程）+（有子系统文档时）同步 `{DOMAIN}.md`。
- DECISIONS.md：独立的决策日志文件（只增不改），先建好空骨架，后续每有关键架构/取舍决策就追加一条。不要把决策日志放进 PLAN.md。
先只产出 PLAN.md 和 DECISIONS.md，不要开始写代码。
```

---

## 阶段 3 · 单任务开发执行(修订原"根据 prd.md 开发")

**改动说明**：原版"根据 prd.md 进行开发"过于笼统，会让 AI 一口气吃下整个项目、上下文爆掉。改为一次只做 PLAN.md 里的任务，强制 commit + 更新接力棒 + 收尾后 /clear。**启动模板按 PLAN 里该任务的档位选——别把所有任务都按重型跑**：分档表立的规矩，靠这里的模板落地（以前只有一个全重型模板，等于分档白分）。

### 3A · 重型任务（命门，Opus + Plan + ultrathink，逐步陪核）

```
读 CLAUDE.md 和 PLAN.md。我们现在只做任务 {T-编号}，不要碰其他任务。

流程：
1. 先在 Plan 模式给出实现计划（涉及哪些文件、关键改动），等我确认。
2. 确认后实现，严格遵守 CLAUDE.md 的约定与禁止项。
3. 实现完成后，自己跑测试和 lint，确保通过。
4. 给我看本次改动的 diff 摘要，以及它如何满足该任务"完成的定义"。
5. 待我确认无误后，提交一次 git commit（message 一句话说清做了什么）。
6. 在 PLAN.md 勾掉该任务；若本任务有关键架构/取舍决策或遗留 TODO，在 DECISIONS.md 追加一行。

注意：只在本任务范围内改动，不要顺手改无关模块。ultrathink
```

### 3B · 常规任务（Sonnet，不 ultrathink，做完看结果）

```
读 CLAUDE.md 和 PLAN.md。只做任务 {T-编号}，不要碰其他任务。
按 PLAN 里该任务的"完成定义"直接实现；只有做法一句话说不清时，才先给我两三行思路确认。
实现后自己跑测试和 lint，绿了就 git commit（message 一句话），并在 PLAN.md 勾掉该任务。
最后给我一个改动摘要：改了哪些文件、如何满足完成定义。只在本任务范围内改动。
```

### 3C · 批量会话（护栏 6 的"批量飞"：几个【可批量】任务连做）

```
读 CLAUDE.md 和 PLAN.md。本会话按依赖顺序连续完成这几个【可批量】任务：{T-a、T-b、T-c}。
对每个任务：实现 → 测试和 lint 绿 → 单独 git commit（一任务一 commit，回滚粒度不能丢）→ 勾掉 PLAN。
全部做完后给我一份汇总：每个任务改了什么、测试结果、有没有偏离完成定义的地方。
中途遇到需要架构决策或契约变更的，停下来问我，不要自作主张。命门任务不在本批，不要碰。
```

（轻型微调不必套模板：直接说要改什么，做完扫一眼；几个小改可共用一个会话，各自 commit。）

> **若任务涉及前端**：所涉"屏/路由"若还没做过附录 B2（计划期设计评审），实现前先做一次，该屏后续任务共享结论；自测后、commit 前接**附录 B3（实现后视觉审计）**对照 DESIGN.md 核对并最小化修复。功能与交互在**同一个任务**里绑定，而不是"功能先做完、体验以后再说"。轻型的文案/样式微调豁免 B2。

完成后你执行 `/clear`，再开下一个会话。

---

## 阶段 3.5 · 里程碑收口：用户旅程走查（每个里程碑必做，别攒到最后）

> 这是 v5 防两张皮的第二道闸。一个里程碑的所有任务做完后、合并/进下一个里程碑之前，**强制以真实用户身份把到目前为止该通的旅程从头走一遍**——验的不是"某个功能对不对"（那是任务级完成定义管的），是"**连不连贯、有没有断头路、像不像一个产品**"。

**为什么必做、且必须每个里程碑做**：脱节只有走整条旅程才暴露。攒到全部做完才走，就是 partygames 那种"M7 才发现像 demo"。每个里程碑都走一遍，断头路在当下就现形。

**走查清单**（对着 PRD 的用户旅程，以新用户视角）：
1. 从落地/入口开始，把本里程碑该支持的那条旅程**完整走通**（不是测单个功能，是走一条链）。
2. 每一步问：我知道现在能干什么吗？下一步的入口清晰吗？这一步和上一步接得上吗？
3. 结束态问：我完成了想做的事吗？我看到结果了吗？有没有理由继续/回来？
4. 交互四态抽查：主要页面的空/错误/加载态，真的触发得到、且不难看吗？
5. 记下所有"断头路 / 不知道下一步 / 信息缺失 / 前后不一致"——这些不是 bug，是**产品连贯性缺口**。

**产出**：一份"旅程走查结果"。缺口按 **断路 > 信息缺失 > 打磨** 排序。断路型（用户根本走不通）必须在进下一个里程碑前补上；其余记入 DECISIONS 或下一里程碑的任务。

**安全门禁（与旅程走查同级的强制项，不是按需）**：本里程碑若涉及**鉴权、支付、PII、文件上传、用户输入入库**中任意一项，收口时必须跑一次附录 A5 安全审计——用子代理或新会话跑，别在实现会话里自己审自己（评审者会继承实现者的全部偏见）。高危项清零才能合并。产品连贯性有 3.5 这道闸是因为被 partygames 疼过；安全不能等疼过才立闸。

> 网页项目可接 **附录 B4（浏览器 QA）** 自动化走一遍，但 B4 偏"找 bug"，这里偏"验产品连贯"，两者互补，别用 B4 替掉人肉走一遍旅程的判断。

---

## 阶段 4 · 上线后迭代：从改动请求到完善落地（v5.1 扩写）

**改动说明**：原版只有"描述 bug→方案→分步改"两步，对上线后的真实迭代不够用——真实的改动请求有大有小、常跨多个模块、且提出时说的往往是"方案"而不是"问题"。本阶段扩成一条分级流水线：**定性 → 变更单 → 按半径分级 → 影响面分析 → 按半径执行 → 回归收口**。微调走完只要几分钟；大改动等价于一次"迷你 0→1"，复用前面全部机制、不另发明流程。

### 4.0 先定性：这是 bug，还是需求变更？

拿到任何改动请求，第一个问题不是"怎么改"，是"**现状和 PRD 谁错了**"：

- **现状偏离 PRD** → 这是 **bug**：PRD 不动，修代码，CHANGELOG 记"修复"。
- **PRD 本身要变**（想要新行为/新功能）→ 这是**需求变更**：先增量改 PRD、再动代码，CHANGELOG 记"需求变更"。
- 分不清 → 按需求变更走 4.1 澄清。

定性决定文档流向。跳过这步的典型事故：把需求变更当 bug 修——代码改了、PRD 没改，下个会话的 AI 对照旧 PRD 又把它"修"回去。

### 4.1 变更单：把模糊请求翻译成可验收的契约（准确理解需求的关键一步）

改动请求天然模糊（"这里不太对""能不能加个 XX"）。凡是**一句话说不清、或改错代价不低**的请求，动手前先用一轮对话把它翻译成**变更单**。在 Plan 模式：

```
我有一个改动请求：{原话描述，可以很糙}

先不要给方案。你的任务是把它翻译成一份"变更单"：
1. 用你的话复述我要什么，并用一个具体例子和我确认："改完之后，用户在 {场景} 做 {操作}，
   会看到 {结果}——对吗？" 复述错了我会纠正你。
2. 如果我说的其实是"方案"而不是"问题"（如"加个下拉框"），先挖回问题本身
   （我到底想解决什么），再判断我给的方案是不是最优解；不是就顶回来，给替代方案。
3. 追问边界：哪些现有行为必须保持不变？空/错误/极端/移动端场景分别怎么办？
4. 对照 PRD.md 判断并告诉我：这是 bug（现状偏离 PRD）还是需求变更（PRD 要改）？
5. 输出变更单：背景与动机 / 期望行为（含具体例子）/ 明确不改什么 /
   验收标准（含反例：什么情况应当被拒绝或报错）/ 涉及哪些用户旅程。
一次问一组问题，别一口气全问。先不要写代码，也先不要给技术方案。
```

> 一句话能说清、验收显然的微调（改文案、调间距、改配置），跳过变更单直接做——变更单是给"说不清、改错代价高"的请求准备的，别让它变成新的仪式税。

### 4.2 改动分级：在复杂度三档之上，加一个"改动半径"维度

0→1 的三档（重型/常规/轻型）看的是"这个任务本身多难"；迭代期还要看**改动半径**——半径决定流程重量，复杂度决定模型档位，两个维度独立：

| 半径 | 判据 | 走什么流程 |
|------|------|-----------|
| **微调** | 单模块内、不动契约/schema、不改既有行为语义（文案/样式/配置/单点小修） | 直接做（轻型节奏），可几个一批，每个各自 commit |
| **常规改动** | 单个功能内、契约只增不改（新增可选字段/新端点）、涉及 ≤2 层 | 4.4 两步流程（方案确认 → 分步实现） |
| **大改动** | 跨 ≥2 个模块 / 契约或数据模型有变更或删除 / 动既有用户数据 / 新增一条完整功能 | 4.5 迷你 0→1（改 PRD → 迭代里程碑 → 阶段 3 执行 → 回归走查收口） |

**硬判据（宁可升级、不可降级）**：只要满足"契约或 schema 有破坏性变更"或"改动跨 ≥2 个模块"任意一条，一律按大改动走，不管它看起来多小——跨模块小改动的事故率远高于单模块大改动。例：一个跨三个模块的字段重命名，是"轻型复杂度 + 大改动半径"：模型用 Sonnet 就够，但流程必须走 4.5。

### 4.3 影响面分析：动手前把"会碰到谁"列成清单（大改动必做；常规改动做简版）

```
基于这份变更单，先做影响面分析，不写任何代码。逐项列出：
1. 受影响的模块/文件（打开代码确认，不要靠记忆猜）；
2. 契约影响：哪些 shared 契约要改？增/改/删各是什么？破坏性变更给出兼容策略
   （契约版本化 / 过渡期双读 / 前后端同批发布，选一并说明理由）；
3. 数据影响：要不要 schema 迁移？既有生产数据要不要回填/转换？给出迁移与回滚预案；
4. 测试影响：哪些既有测试会红？改动区现有测试覆盖够不够，不够先补什么再动刀；
5. 旅程影响：哪些既有用户旅程穿过改动区（这个清单就是收口时回归走查的范围）；
6. 文档影响：PRD 哪几节要增量修订。
输出"影响面清单"等我确认。若读代码后发现实际影响比变更单预估的大，先停下来告诉我，
可能需要重新分级。ultrathink
（常规改动的简版：可去掉 ultrathink，清单只做 1/2/4 三项。）
```

**跨模块改动的执行顺序沿用契约先行**：先在 shared 包把契约变更定稿 → 迁移（如有）→ 各模块按依赖序逐个适配 → 前端最后。多个模块**不要在一个会话里并行乱改**：一个会话改一个模块，契约 + PLAN 是会话之间的接力棒。

### 4.4 执行 · 常规改动（原阶段 4 主体，保留）

**第一步**——Plan 模式给方案：

```
{贴入变更单，或直接描述需求/bug}

请给出解决方案，画出受影响的部分（原型 + 技术方案），并仔细检查不要影响非相关模块。
确认按你的方案实现后能完整满足变更单的验收标准。如有多个方案，列出各自取舍供我选择。
```

**第二步**——你确认方案后，切到执行：

```
就用方案 {编号}，版本号设为 {x.y.z}。

1. 先更新文档：PRD.md 对应章节增量修订，保持"当前真相"（逐段改，禁止整文件重生成）；
   CHANGELOG.md 顶部追加本次条目（需求变更/修复、受影响模块、技术要点）。
2. 把代码改动切分为合理的 todolist，按顺序执行。
3. 【关键】每完成一项，给我看 diff，我确认后再继续下一项；不要一次性全改完。
4. 全部完成后跑测试和 lint。若有失败：修复一轮，然后停下来把问题和修复方案告诉我，
   由我决定是否继续——不要进入"自动修到通过"的循环，更不要通过删除报错的功能或测试来让检查通过。
5. 确认通过后 git commit。

【仅网页项目保留】可调用浏览器工具（Chrome DevTools MCP / Claude in Chrome）打开页面检查并报告问题；
但只报告、由我决定修复范围，不要无限自动修复。

请再次确认：按此实现后能完整满足变更单，且"明确不改什么"清单里的行为一个都没被影响。
```

### 4.5 执行 · 大改动 = 迷你 0→1（跨模块 / 动契约 / 新功能一律走这条）

大改动不要在一个会话里"聊着聊着就改了"。它就是一个小号的 0→1，前面所有机制原样复用：

1. **PRD 增量修订**：按变更单增量改 PRD 对应章节。新功能必须补齐用户旅程/交互四态/验收三轨——和 0→1 同样的产品完整度要求。**迭代期加的功能最容易变成新的"两张皮"**：主流程当年走过全套三轨验收，后加的功能全靠"顺手加上"，几轮迭代后产品又碎了。
2. **拆迭代里程碑**：在 PLAN.md 追加一个迭代里程碑 `M-v{x.y} · {用户能完成的事}`，规则与阶段 2 完全相同：按垂直切片切、切片内部按 DB→逻辑→契约→前端 排序、每任务标【复杂度】【执行模式】、末尾带收口验收门任务。
3. **按阶段 3 执行**：一会话一任务、批量飞命门慢、8 条护栏全部生效。
4. **收口 = 新旅程走查 + 既有旅程回归走查**：阶段 3.5 照做，但走查范围是"本次新旅程 + 影响面清单第 5 项列出的全部既有旅程"。**迭代期的断头路大多不在新功能里，而在新功能把旧旅程撞断的地方**（改了导航结构、占了原有入口、改了旧接口语义）。
5. 涉及鉴权/支付/PII/既有用户数据迁移的大改动，收口前 **A5 安全审计必做**（不是按需）；迁移类改动上线前先备份数据。

### 4.6 版本号与文档同步（每次改动的收尾铁律）

- **版本号跟半径走**（沿用原约定）：微调 → 改第三位 `1.0.x`；常规改动 → 改第二位 `1.x.0`（如 1.2.0）；大改动 → 改第二位，含不兼容变更的重大升级才改第一位 `x.0.0`（如 2.0.0）。
- **文档和代码同批 commit**：每次改动落地 = 代码 + PRD 增量修订（如有）+ CHANGELOG 顶部条目 + DECISIONS 追加（如有取舍决策）+ PLAN 勾选（大改动）在同一批提交里完成。文档和代码分批提交，就是埋下"下个会话的 AI 按旧文档把改动改回去"的种子。
- 微调可攒批：几个微调做完，CHANGELOG 合并记一条 `1.0.x`。

### 4.7 Phase 模式：成体系的多里程碑建设（大改动的放大版）

**何时升级**：一个子系统要连做 ≥2 个相关里程碑（整套经济系统、整个支付体系、一次大重构），别按零散大改动逐个走 4.5——升格为一个 **Phase**。实证模式来自真实项目的 Economy V3（Phase 1a–3b，跨 7 个里程碑规划一次、逐里程碑交付）。

1. **Phase 开工 = 规划拆解会话**：产出 ① Phase 设计文档（提交入库，禁止游离在 git 外）；② 里程碑序列（每个仍是垂直切片，PLAN 追加占位行）；③ 关键取舍记 DECISIONS。涉运营数值的，在各里程碑 T0 设数值拍板门。
2. **执行 = 各里程碑照常走里程碑循环**：阶段 3 / 3.5 全套不变——Phase 是规划层级，不豁免任何门禁。
3. **Phase 收口 = 最后一个里程碑收口之上加三件事**：① 跨里程碑总 E2E（整个 Phase 的旅程连起来走一遍）；② 子系统参考文档整体同步 + PRD 骨架层增量修订（防止"上线后 PRD 停在 MVP、真相散落各处"——这是真实项目的实际结局）；③ 归档：本 Phase 的 PLAN 任务清单与 DECISIONS 条目各自入 ARCHIVE 卷。

---

## 阶段 5 · 跨模型评审(保留并强化原"进阶2")

原套路最有价值的技巧之一：A 模型写需求/方案，换 B 模型评审并优化，再让 A 开发。不同模型盲区不同（你的截图里 Codex 就抓出了"匿名 Supabase client 在 RLS 下写不进、需用 service_role"这类安全问题）。

把 PRD.md（或某版本方案）交给另一个模型，提示：

```
请你作为资深 CTO / 安全工程师，审查这份需求/技术方案能否真正解决问题，重点检查：
1. 业务逻辑是否正确、有无遗漏的边界情况？
2. 安全与权限：鉴权、数据写入边界、密钥处理是否有隐患？
3. 数据契约：字段类型、数据库 schema、类型定义是否自洽？
4. 产品体验：用户旅程是否连贯、有无断头路？每个核心功能的交互四态（加载/空/错误/成功）是否都定义了？
5. 是否有未被发现的潜在问题？
6. 文档是否清晰，能否让另一个开发者快速理解？

输出评审报告：问题清单（按严重度排序）+ 逐段修改建议（指明改 PRD 哪一节、怎么改）。
不要直接重写整份文档——修改由原模型按"增量编辑、逐段修改"落回 PRD，禁止整文件重生成。
注意：目标是让方案更稳，不要为了"看起来通过"而删减必要功能或测试。
```

优化后的文档再交回 A 模型按阶段 3 / 4 开发。

---

## 阶段 6 · 实战护栏（partygames 血泪换来的 8 条 + 1 条并行模式，写进 CLAUDE.md）

> 这些不是提示词，是真实项目里踩过、代价很大的坑，固化成规则/习惯能让执行顺畅得多。前 5 条建议直接写进 CLAUDE.md（硬规则/易错点），后 4 条是执行节奏与手法。

### 护栏 1 · 合并前必须确认 CI 全绿（最痛的一条）

**坑**：CI 可以红着被合并进 main，且没人发现——曾有项目 main 从某里程碑起 CI 一直红、连续四个里程碑带红合并都没察觉（本地总绿，是 dist/.env 残留掩盖了 CI 真实状态）。

**规则**（写进 CLAUDE.md TIER 1）：合并任何 PR 前，必须确认 CI 全绿（All checks passed），红绝不合并。若有 `gh` CLI，合并前先 `gh pr checks` 贴出状态；AI 在建议"可以合并"之前必须主动查 CI 并报告，红就先修不建议合。
> 免费私有仓库开不了分支保护（要 Pro 或公开仓库）时，这条全靠人+AI 纪律兜底，更要写死。能开分支保护（require status checks）就开，从机制上杜绝带红合并。

### 护栏 2 · 切下一里程碑前，确认上一里程碑"全部" commit 都进了 main

**坑**：一个里程碑分两次 PR 合并时，容易漏掉第二批——先合了主体，之后又追加的任务/审计/修复 commit 忘了再开 PR 合并，导致下一里程碑基线缺代码。

**规则**：切任何新里程碑分支前，`git log origin/main` 确认上一里程碑的**全部** commit 都已进 main（不是"看到一次 Merge"就以为完整）。里程碑首次合并后又追加的 commit，必须再开一个 PR 合并。同时 `gh run list --branch main` 确认 main 自身的 CI 是 success——commit 齐了但 main 的 CI 红着，一样不准起跑。

### 护栏 3 · 本地绿 ≠ CI 绿，用"本地模拟 CI"验证

**坑**：本地跑测试/lint 全绿，CI 却红——因为本地有构建残留（dist）、有 `.env`，掩盖了"全新环境跑不起来"的问题。

**规则**：怀疑 CI 会红、或改了构建/依赖/生成步骤时，本地模拟 CI 环境验证（清掉 dist/generated/缓存 + 临时无 `.env`，按 CI 步骤顺序跑一遍），全绿再推。别改完直接推、等 CI 告诉你对不对。

### 护栏 4 · 改结构化文档只增量、逐段移动，改完 git diff 核验

**坑**：让 AI 改 PLAN/PRD 时"重新生成整个文件"，极易丢任务、抹掉 checkbox（曾两次损坏 PLAN：整文件重写丢内容、编辑器 Format-On-Save 抹掉全部复选框）。

**规则**：改 PLAN/PRD/DECISIONS 一律**增量编辑、逐段移动，禁止整文件重生成**；归档只"移动"不"删除"；改完 `git diff` 核对 checkbox 数量与结构没被动。（编辑器的 Format-On-Save 对这类文件建议关掉。）已完成里程碑的任务清单整段剪切移入 `PLAN-ARCHIVE.md`、PLAN 里留一行指针，同样只移不删、diff 核对。DECISIONS 满一个 Phase 整卷移入 `DECISIONS-ARCHIVE.md`，活跃文件只留索引 + 当前 Phase 条目（见文档分工·文档规模化护栏）。

### 护栏 5 · 接口隔离变化：签名为"未来的实现"而定，上层零改动

**坑/智慧**：一个会被替换实现的能力（如"AI 走子"以后要从本地换成远程服务、"读评分"以后要从默认值换成查数据库），如果一开始签名就为"未来那个实现的形态"而定（比如统一定成异步 `Promise`），将来换实现时上层调用零改动；反之图省事定成同步，换实现时全链路要改。

**规则**：识别"现在先给个占位实现、以后要换真实现"的能力，把接口签名按**未来实现的形态**定稳（DB 读/网络调用→异步），中间实现用占位（返回默认值/常量），换真实现时只改内部、不动接口和上层。这是让"后续里程碑零改动接上"成立的关键手法。

### 护栏 6 · 执行节奏：批量飞 + 命门慢

单人开发时，按任务风险分两种节奏跑，别一刀切：
- **【可批量】低风险任务**（契约骨架、常规端点、铺 UI、接入壳）：一个会话里按依赖顺序连续做几个、做完汇总一次给你看，不必每个都停下确认。每个任务仍各自 commit（回滚粒度不能丢）。
- **【单独+确认】命门任务**（鉴权、实时同步、状态机、数据模型、核心算法、动既有核心时序、动钱（支付/账本/发放）、外部 webhook 入口）：单独一个会话做、做完停下人工核验，再进下一个。
> 分档表已给复杂度维度；这一条是"怎么把多个任务编排进会话"的节奏。命门绝不混进批量飞。

### 护栏 7 · 命门连跑没逐个核时，合并前对最危险的命门做针对性自审

**坑**：把几个命门一口气做完才汇报（没逐个陪核），其中的隐蔽 bug（并发竞态、失败降级、状态机边界）正常测试和手验都撞不到。

**规则**：若命门是连跑完成的，合并前对最危险的那一两个做一次针对性自审（只读、给证据 file:line + 覆盖测试名），重点扫：服务端权威有没有信客户端、失败路径的降级、并发/时序竞态、既有测试是否仍绿没被改坏。零高危再合并。（用附录 A3 偏执评审 / A5 安全审计的角色。）

### 护栏 8 · 伏笔管理：延后的决策要留痕、标归属

**坑**：一个"现在先不做、以后某里程碑再做"的决策（如"计时先预留字段、判负逻辑放到后面某里程碑"），若不留痕，到时容易忘了它存在、或忘了当初为什么这么定。

**规则**：任何"预留/延后"决策，当场在 DECISIONS 记一条：延后什么、归属哪个里程碑、当前先怎么占位（如"契约预留字段、发 null"）、为什么。相关文档处加交叉引用（如 PLAN 的范围边界写"见 D××"）。这样跨里程碑的伏笔到兑现时有据可查、零改动接上。

### 护栏 9 · 双轨并行：切片内前后端两条泳道（契约冻结后起跑）

**场景**：前端的产品定义/设计打磨慢（人重、AI 轻），后端批次很快跑完（AI 重、人轻），后端产能空闲。**能并行，但并行的单位是"一个切片内的两条泳道"，不是"前后端两条独立产品线"**——后者就是 v5 根治的按技术层推进，会把两张皮从后门放回来。

**规则**：
- **起跑线 = 契约冻结**：该切片的 API 契约（含错误码）在 shared 包定稿（命门任务、人工确认）后，前后端两泳道解耦、各自按契约推进。B2 屏级评审就是前端泳道的"契约冻结"。
- **后端先行的上限 = 本切片 + 下一切片【设计无关】任务**（算法/纯逻辑/已确认实体 schema）。**绝不预做未来旅程的 API**——聚合粒度、权限边界、状态机全派生自旅程，旅程未定先写的接口多半"字段都对、形状全错"。预做的核心逻辑按护栏 5 把签名定成未来形态，兑现时零改动接上。
- **DoD 不动摇**：里程碑完成 = 旅程走通。后端泳道跑得再快也只是任务勾选，不存在"后端完成"这种里程碑状态；切片不合并、不进 CHANGELOG，直到前端汇合、3.5 走查过门。
- **汇合 = 联调**：收口验收门之前固定一个"按旅程联调"任务——错误码→错误态映射、加载时序、边界值；两端各自对契约绿不等于拼起来对（可用 B4 辅助）。
- **空档填法**（后端跑完、前端未就绪时，按价值排序）：① 下一切片【设计无关】核心；② 质量债（命门补 A3、补测试矩阵、跑性能基线）；③ 线框级占位 UI 先把旅程走通（丑但能走，提前暴露断头路），视觉等 DESIGN.md 定稿后按 B3 一轮补齐。唯一不该做的：闲着难受去写未来的接口。
- **单人形态的"并行"= 错峰人机负载**：你磨设计决策（看原型、定 B2、改 DESIGN.md）的同时，让 AI 会话跑后端批次——人的思考时间和 AI 的执行时间重叠，而不是串行相加。要物理并行就开两个 git worktree 各一个会话，但 **shared 契约包只允许一条泳道动**，契约变更永远是命门。

---

## 附录 · 从 gstack 吸收的领域无关角色(适配后端/算法)

> 以下提示词取自 Garry Tan 的 gstack 工作流中**与前端/浏览器/macOS 无关**的部分，
> 改写成不依赖 gstack 安装的纯提示词，可直接在 Claude Code 里用。
> 它们是对前面阶段的"增强插件"，按需取用，不必全用。

### A1 · 问题重构(用在阶段 1 之前，避免按字面实现)

最容易被忽略、价值却最高的一步：动手前先逼模型质疑你的框定。在 Plan 模式：

```
在我们开始规划之前，先扮演一个会顶回我框定的资深产品/技术合伙人。不要按字面接受我的需求。
1. 用具体的提问挖掘我真正的痛点（要具体例子，不要假设）。
2. 如果你认为我描述的痛点指向一个比我说的更有价值的产品，直接顶回来并说明。
3. 给出 2-3 个可证伪的"产品前提"，让我逐条确认/反驳——我接受的每条前提都会成为后续设计的承重墙。
4. 给出 2-3 个实现路径，各带诚实的工作量估计，并推荐其中一个及理由。
先不要写任何代码。ultrathink
```

### A2 · 工程评审 + 强制画图(增强阶段 1，对后端/算法最有用)

gstack 里"工程经理模式"的核心洞察：**逼模型画图，它的方案会完整得多——图会把隐藏假设逼到明面上。** 在 Plan 模式，对着已确认的方案：

```
扮演我最好的技术负责人，对当前方案做工程评审，目标是"让它可被构建"。逐项给出：
- 架构与系统边界、模块职责划分
- 数据流与状态转换、信任边界
- 失败模式与边界情况：某步成功但下游失败怎么办？低置信度/部分失败如何降级？
- 重试、幂等、防重复任务怎么做？什么该持久化、什么可安全重算？
- 测试矩阵：关键路径 × 状态的覆盖

【强制】用 Mermaid 画出：序列图、状态图、数据流图、组件图中所有适用的。
画图过程中暴露出的任何隐藏假设，单独列出来让我确认。
先不要写代码。ultrathink
```

> 对你的 Gomoku AI / 推理服务这类逻辑密集的东西，这一步尤其值——算法的状态转换、并发、失败降级最容易被口头规划糊弄过去。

### A3 · 偏执评审(用在阶段 3 实现之后，找"能过 CI 却炸生产"的 bug)

```
扮演一个偏执的资深工程师审查这次改动。测试通过不代表安全。专门找这类能逃过 CI、却在生产爆炸的问题：
- N+1 查询、陈旧读、竞态条件、死锁
- 信任边界错误、缺索引、转义/注入 bug
- 被破坏的不变量、错误的重试逻辑
- "通过了但漏掉真正失败模式"的测试
- 被遗忘的枚举/状态分支：如果新增了一个状态常量，追踪它是否在每个 switch/白名单里都处理了

机械性的明显问题直接修并标注 [已修复] 文件:行。
涉及安全/竞态/设计取舍的，单独列出来交给我决定，不要自作主张。
```

### A4 · 纪律化调试(卡住找不到原因时，避免"再试一下"的瞎改循环)

gstack 的"铁律：未查清根因前不许改"，能挡住最浪费时间的瞎试：

```
现在进入调试模式，遵守铁律：未查清根本原因前，不许做任何修复。
1. 先追踪数据流，定位问题真正发生在哪里。
2. 提出 1-2 个可证伪的假设，一次只验证一个。
3. 找到根因后，先把根因讲给我听、再动手修。
4. 如果连续 3 次修复都失败，停下来，转而质疑架构本身，不要继续在同一个方向硬试。
（可选）调试期间把可写范围限制在 {出问题的模块目录}，避免误改无关代码。
```

### A5 · 安全审计(对你的 API 服务/推理业务尤其相关)

```
扮演首席安全官，对这个代码库做一次 OWASP Top 10 + STRIDE 威胁建模审计。逐项扫描：
注入、失效的鉴权、敏感数据暴露、失效的访问控制、安全配置错误、XSS、
不安全的反序列化、已知漏洞组件、日志/监控不足。
每个发现给出：严重级别、证据（文件:行）、建议修复。
不要顺手改代码，先给我完整报告。
```

### A6 · 跨模型对抗评审(增强阶段 5 的"挑战"模式)

阶段 5 是常规评审，这里加一个**对抗模式**——让另一个模型主动"试图弄坏"你的代码：

```
不要做常规评审，进入对抗模式：主动尝试弄坏这段代码。
找边界情况、竞态条件、安全漏洞、在高负载下会失败的假设。用最大推理强度。
把它当成对你逻辑的渗透测试。列出每个能让它崩的具体场景。
```

> 跑完后，把它和 Claude 自己评审的结果对比：**两边都报的几乎一定是真问题；各自独有的，是对方的盲区。**

### A7 · 操作安全网：能机制化的不靠纪律(贯穿全程)

写在 CLAUDE.md 里的纪律会在长会话尾部失效。护栏 1 的原则——"能开分支保护就开，从机制上杜绝"——应该用到所有约束上：**同一条规则，优先用工具硬拦截，CLAUDE.md 的纪律只做兜底**。

- **危险命令 → permissions 硬拦截**：在 `.claude/settings.json` 的 permissions 里把 `rm -rf`、`git push --force`、`git reset --hard`、`DROP TABLE` 类命令设为 deny/ask（或用 PreToolUse hook 拦截），而不是只在 CLAUDE.md 里"要求 AI 先确认"。CLAUDE.md 那条保留作兜底（常规 `node_modules`/`dist` 清理放行）。
- **密钥防提交 → pre-commit + CI 扫描**：secret 扫描（gitleaks 类）装进 pre-commit 和 CI，"禁止提交密钥"从嘴上纪律变成机器门禁；依赖漏洞审计（`npm audit` 或等价物）进 CI。这两项写进 M0 的 DoD（见阶段 0）。
- **高频角色提示词 → 自定义命令**：附录 A/B 这些反复用的评审角色，做成 `.claude/commands/` 自定义斜杠命令（如 `/paranoid-review`），一次定义、随处调用，杜绝手工粘贴时粘错版本、粘漏约束。Claude Code 内置的 `/security-review`、`/review` 能覆盖的直接用内置，别重新发明。
- **评审 → 子代理/新会话跑**：A3 偏执评审、A5 安全审计不要在实现代码的同一会话里跑——评审者会继承实现者的全部上下文和偏见（等于自己审自己），还烧主会话上下文。用子代理（subagent）或 /clear 后的新会话跑：干净上下文 + 天然的另一双眼睛。
- **编辑边界锁定**：调试某模块时，明确告诉它"本次只允许改 {目录}，不要碰其他目录"，防止它顺手"修好"无关代码。
- **上层目录 CLAUDE.md 检查**：Claude Code 会向上递归加载父目录的 CLAUDE.md——仓库上层若遗留旧宪法/模板，会污染并顶撞本项目规则。开项目前检查一次（此坑也写进 CLAUDE.md 易错点）。

**存量项目补装**：跑了很久、机制层为零的项目（真实案例：356 个 commit 的项目 permissions 里没有任何 deny、CI 无 secret 扫描，全靠 CLAUDE.md 纪律），补救是一个一次性【常规】任务："补装机制层——① `.claude/settings.json` 危险命令 deny/ask；② pre-commit + CI 加 secret 扫描（gitleaks 类）；③ CI 加依赖审计；④ 高频评审角色落 `.claude/commands/`；⑤ 仓库根放 SELFCHECK.md，下个会话起用。"补装完成前，SELFCHECK 的机制层审计（R5.4）持续报违规。

### A8 · 支付 / 涉钱集成检查单（接支付商、webhook、账本类功能必过）

动钱功能全部按命门走（重型 +【单独+确认】），并逐项核对：

1. **webhook 入口三件套**：验签（拒绝未签名/错签名请求）+ 幂等（同一事件重放不重复发钱）+ 回传定位字段实证（metadata 能唯一定位到订单/用户——用真实回传验证过，不是"文档说可以"）。
2. **双轨 provider**：CI 与本地永远跑 fake provider；真实 provider 只在部署环境经 env 切换。fake 轨是回归测试的地基，接真轨后不许删。
3. **测试对支付配置封闭**：测试套件不得读取开发者 `.env` 里的真实支付凭证（实证事故：本地 .env 的支付商凭证泄入测试套件，测试行为随开发者环境漂移）。
4. **上线前真链路实证**：小额实付走通 下单→回调→验签→发放→对账 闭环，留痕。
5. **改价/换商品兼容**：旧产品退市后仍可解析（历史订单/回调引用旧 ID 不得报错）；价格数值先过拍板门（阶段 2）。
6. **账本可对账**：每笔发放有账本记录（reason + 来源事件 ID），能回答"这笔钱为什么发"。

---

## 附录 B · 前端 / UI / 浏览器 / 部署角色(全栈 web 应用)

> 你的项目有前端、后端、算法、数据库四层。附录 A 的角色（工程评审、偏执评审、调试、安全）四层通用；
> 这一节补上前端与浏览器相关的角色。标注【需浏览器】的，接 Claude in Chrome 或 Chrome DevTools MCP 使用。

### 全栈分层的推进顺序(已提升为主流程阶段 2 的拆任务规则，此处留细节备查)

四层不要平行乱开，按依赖顺序推进，用"契约"把层与层解耦：

```
1. 数据库 schema / 数据模型      ← 先定，它是地基
2. 算法 / 核心业务逻辑           ← 可独立于 API 单测
3. 后端 API（定义接口契约）      ← 前后端的解耦点
4. 前端（按 API 契约对接）
   ↑ 设计系统(B1) 和 计划期设计评审(B2) 在写前端代码前先做
```

**关键**：第 3 步先把 API 契约（路径、入参、出参、错误码）在 **shared 契约文件（zod/schema）里定稿——契约的单一真相是 shared 包里的代码，PRD 只放指针（文件路径）和语义说明，不复述字段细节**（两处各写一份，必然漂移打架）。契约定稿后，前端和后端就能各自按契约推进、互不阻塞——这也让你前面"按模块拆会话"的做法在全栈下成立（一个会话做一层/一个端点，而不是一会儿前端一会儿后端把上下文搅乱）。

### B1 · 设计系统咨询(写前端代码之前，建立 DESIGN.md)

> 已完整并入主流程**阶段 1.5**（带 PRD 设计方向 + 设计输入素材的加强版），直接用那一版。此处不再保留旧文本副本——同一提示词存两份，各自演化必然打架（保留编号只为不破坏引用）。

### B2 · 计划期设计评审(写前端代码之前，补全 UI 盲区)

> 此提示词已是主流程**阶段 2/阶段 3 前端任务**的标准一环（不是可选）；这里是完整版。

大多数计划只写了后端做什么，却没写用户看到什么——空状态、错误态、加载态、移动端布局全被推到"实现时再说"。在 Plan 模式对着前端任务的计划：

```
扮演资深设计师评审这份前端计划（先不写代码）。逐项打分 0-10，说明 10 分长什么样，然后直接改进计划：
1. 信息架构：每屏的主/次/三级内容层级
2. 交互状态覆盖：每个 UI 功能的 加载/空/错误/成功 四态是否都定义了
3. 用户旅程是否连贯
4. AI-slop 风险：是否充斥"渐变 hero + 三列图标卡 + 统一圆角"这类一眼 AI 生成的套路，给出更有意图的替代
5. 设计系统一致性（对照 DESIGN.md）
6. 响应式 / 可访问性
7. 尚未拍板的设计决策
明显的缺口直接补进计划；真正的取舍列出来让我选。
```

### B3 · 实现后视觉审计 + 修复【需浏览器】

前端实现完，对着跑起来的页面做视觉体检并修复：

```
对运行中的页面 {URL} 做视觉审计：逐项检查排版层级、间距一致性、对齐、空/错/加载态、
移动端断点、AI-slop 痕迹、对照 DESIGN.md 的偏差。
然后进入修复循环：每个发现定位到源文件，做最小改动，一个 commit 修一个问题
（message 形如 style: 修复xxx），改完重新加载页面核对，给我看前后对比。
纯 CSS 改动可直接修；改到组件 JSX/TSX 逻辑的，先停下来问我。一次最多修 30 处，超出风险预算就停下。
```

**布局程序化断言（每个 UI 改动任务的完成定义，不依赖专门的审计会话）**——测试/lint/typecheck 全绿抓不到裁切、横向溢出、错位。凡渲染或改动了界面（组件、CSS、弹框/卡片/行内加字段）的任务，commit 前在 preview 里跑三条断言：
1. 无横向溢出/裁切：`document.documentElement` 与改动容器的 `scrollWidth - clientWidth === 0`；最右侧元素 `getBoundingClientRect().right` 不超容器右缘。
2. 内容与标题/兄弟元素对齐：一致内边距、行内 label 与值不相撞。
3. 375px 移动视口重跑上述断言；取内容最宽的那一行重点验（最先裁切的就是它）。
实证：购买确认框双币金额贴右缘被圆角裁切——四命令全绿，界面是破的。

### B4 · 浏览器 QA【需浏览器】(对全栈最有用)

不只是看前端，是端到端验证真实流程能不能跑通：

```
对当前分支做 QA：先读 git diff main 找出本次改动影响了哪些页面/路由，启动浏览器逐个测试，
填表单、走关键流程（如注册→登录→核心操作），找出 bug。
每个 bug：定位源文件、最小改动修复、一个 commit 一个修复、重新验证，并为每个修复自动补一条回归测试。
最后给我一份带截图的报告 + 健康分。
（测试需要登录的页面时，用专用测试账号登录——不要导入或使用我真实账号的 cookie/会话凭证，
把真实凭证交给自动化代理是安全反模式。）
```

> 这一步把前端 bug 和后端契约不匹配的问题一起暴露——全栈联调最容易出问题的地方。

### B5 · 性能基准【需浏览器】

```
对 {URL} 的关键页面建立性能基线：加载时间、Core Web Vitals（LCP/CLS/INP）、资源数与总传输大小，
每页多跑几次取平均。保存基线，改动后再跑一次对比，标出回归。
```

### B6 · 发布与发布后监控(全栈通用)

```
【发布】这个分支已就绪：同步 main、跑全部测试、审计本次 diff 的测试覆盖（缺的补上）、
推送并开 PR，PR 描述里写清覆盖变化。先不要合并。

【上线 + 验证】合并 PR，等 CI 通过，等部署完成，然后对生产环境跑健康检查
（关键页面是否 200、有无 console 报错、p95 延迟）。若部署失败，告诉我失败点和是否需要回滚。

【发布后监控】【需浏览器】循环巡检关键页面，盯 console 报错、性能回归、页面失败，
对比上线前基线，发现异常立即报告。
```

---

## 边界与原则

- 这套提示词只提高模型写代码的**下限**（错误更少），不能逆天改命让 AI 无所不能；仍会出错，需要你判断。
- **它的价值建立在"你能判断 AI 做得对不对"之上**：它砍掉/简化某处时，你要能分辨是合理精简还是缺功能；看不懂为什么这么写，就直接问它"为什么这样就够了"。
- 自动化越强，越要给它**回滚点**(git commit)和**停止点**(改一轮就停下让你看)，而不是放它无监督地长跑。
- 上下文是有限资源：一个会话只做一个任务，靠 PLAN.md + CHANGELOG + git 接力，而不是靠会话记忆。

---

## 一页速查

```
核心原则：里程碑 = 用户能完成的"垂直切片"（前端→API→逻辑→数据的最小可玩闭环），
          不是"一块技术"。分层只用于切片内部。← 这是防两张皮的根

阶段0 初始化  → 立 CLAUDE.md（项目宪法，含阶段6的8条护栏）
阶段1 规划    → 出 PRD.md：功能（业务规则/数据契约）+ 产品（用户旅程/页面清单/交互四态/设计方向）
                验收三轨：功能轨 + 体验轨（单页四态/响应式/a11y）+ 旅程轨（跨页连贯/无断头路）
阶段1.5 设计  → 建 DESIGN.md 并回填 CLAUDE.md；设计重的产品用 Claude Design 出原型
阶段2 拆任务  → 出 PLAN.md：里程碑按垂直切片切、切片内部按 DB→算法→API契约→前端 分层；
                每里程碑写"可玩完成定义"+ 末尾收口验收门（含归档/删分支/同步子系统文档）；
                每任务标【复杂度】【执行模式】【泳道】；涉运营数值的里程碑 T0=数值拍板门；
                前端完成定义含交互四态+布局断言；B2 以屏为单位（+ DECISIONS.md 独立）
─────────── 每个里程碑循环 ───────────
阶段3   执行  → 按档位选模板：3A 重型(Plan+ultrathink+逐步陪核) / 3B 常规(直接做、做完看) /
                3C 批量会话(可批量任务连做、一任务一commit)；轻型微调不套模板
                前端：屏级 B2（首个任务前一次），自测后接 B3，再 commit→勾PLAN→/clear
阶段3.5 收口  → 里程碑末尾以新用户身份走一遍完整旅程，验连贯/断头路（别攒到最后！）
                断路型缺口进下一里程碑前必补；涉敏里程碑（鉴权/支付/PII/上传/输入入库）
                A5 安全审计必做（子代理跑，高危清零才合并）；CHANGELOG 记 0.{n}.0
────────────────────────────────────
（上线后）
阶段4 迭代    → 定性(bug↔需求变更) → 变更单(复述+例子+不改什么+验收含反例) → 按改动半径分级：
                微调直接做 ｜ 常规两步(方案→分步改，每步确认) ｜ 大改动=迷你0→1
                （影响面分析→增量改PRD→迭代里程碑M-v{x.y}→按阶段3执行→新旅程+既有旅程回归走查）
                硬判据：动契约/schema 或 跨≥2模块 → 一律大改动；涉敏大改动收口前 A5 必做
                4.7 Phase 模式：子系统连做≥2里程碑 → 开工规划拆解(设计文档入库+拍板门)
                → 各里程碑照常循环 → Phase 总收口(跨里程碑E2E+子系统文档同步+归档)
                收尾铁律：代码+PRD+CHANGELOG+DECISIONS 同批 commit；版本号跟半径走
阶段5 评审    → 换模型审查（安全/数据/边界 + 产品体验：旅程连贯/四态完整），优化后再开发
阶段6 护栏    → 9 条实战规则（CI绿才合并/尾巴漏合/本地≠CI/PLAN护栏/接口隔离/批量飞命门慢/
                合并前自审/伏笔管理/双轨并行·契约冻结后起跑）

主轴（切片内部，贯穿阶段1.5→3）：
  数据库 → 算法 → 后端API契约 → 前端（写前端前先做 DESIGN.md/B1 + 计划期设计评审/B2）

附录 A（四层通用，按需取用）：
A1 问题重构  A2 工程评审+强制画图  A3 偏执评审  A4 纪律化调试
A5 安全审计  A6 跨模型对抗评审     A7 机制化安全网(permissions/pre-commit扫描/自定义命令/子代理评审/存量补装)
A8 支付集成检查单(webhook验签+幂等/fake双轨/测试配置封闭/实付E2E/退市兼容/账本对账)

附录 B（前端/浏览器细节，主流程已引用 B1/B2，【浏】=需浏览器工具）：
B1 设计系统  B2 计划期设计评审  B3 视觉审计修复【浏】
B4 浏览器QA【浏】  B5 性能基准【浏】  B6 发布与监控
```
