# PRD.md · 产品需求规格（模板 · 配套工作手册 v6）

> 活文档：直接修改保持"当前真相"，不堆历史版本（历史进 CHANGELOG.md）。
> 由手册阶段 1 的提示词生成后，人工审定。以下是产出物的结构骨架。
> **两层结构**：§1–§10 是骨架层（稳定、少改）；§11 是里程碑细节层，每个里程碑**开工前**在此 JIT 补齐字段级规格——不要在阶段 1 一次写全（长项目必然失真）。
> 数值密集型子系统（经济/数值表）不放这里：另开 `{DOMAIN}.md` 子系统参考文档（代码为真相源的同步镜像，见手册文档分工）。

## 1. 核心目标 (Mission)
{一句话描述最终愿景}
- **成功指标（可度量）**：{如：新用户完成核心动作的比例 / 次周回访率 / 核心操作 p95 延迟——没有度量，上线后的迭代方向只能靠感觉}

## 2. 用户画像 (Persona)
- 为谁设计：{…}
- 核心痛点：{…}

## 3. 版本规划
### V1 / MVP（垂直切片式里程碑）
> 每个里程碑 = 用户能完成的一件完整的事（可玩竖切），不是一块技术。
> 每条写清【DoD·可玩定义】：一个新用户能用它完成什么、看到什么。

- M0 · 工程地基（唯一允许的非切片里程碑）——DoD：{仓库/CI/脚手架就绪}
- M1 · {用户能完成的第一件事}——DoD：{新用户打开 → 做什么 → 看到什么结果}
- M2 · {…}——DoD：{…}
- …

### V2+（未来版本）
- {…}

## 4. 关键业务逻辑 (Business Rules)
- BR-1：{规则}
- BR-2：{…}

## 5. 数据契约 (Data Contract)
{核心实体关系与语义说明。**字段级契约的单一真相是 shared 契约文件（zod/schema）里的代码，此处只放指针（文件路径）**，不复述字段细节——复述必漂移}

## 6. 产品体验（必填，四件套）

### 6.1 用户旅程 (User Journey)
> 每条业务规则翻译成"用户从哪进入→走哪几步→每步看到什么反馈"；确保没有断头路。
```mermaid
flowchart LR
  落地 --> 决定做什么 --> 核心动作 --> 看到结果 --> 回访
```
- 阶段 1 落地：{用户看到什么、第一步做什么}
- 阶段 2 …：{…}
- 结束态：{用户完成了什么、为什么想再来}

### 6.2 页面/路由清单 (Screen Inventory)
| 路由 | 用途 | 主信息 | 次信息 | 三级信息 |
|---|---|---|---|---|
| / | {…} | {…} | {…} | {…} |

### 6.3 关键交互状态 (Interaction States)
> 逐个核心功能定义四态，挂回业务规则与数据字段。

| 功能 | 加载 | 空 | 错误 | 成功 | 关联 BR/字段 | 错误码→错误态映射 |
|---|---|---|---|---|---|---|
| {…} | {…} | {…} | {…} | {…} | BR-x / {…} | {API 错误码 ↔ 展示什么（i18n key）} |

> 错误码→错误态这一列是前后端联调最常断的线：API 契约定义了错误码（后端侧）、四态定义了错误态（前端侧），必须在这里挂上，否则各做各的。

### 6.4 设计方向 (Design Direction)
产品要传达：{信任/速度/质感/温度…}（作为阶段 1.5 建 DESIGN.md 的输入）

## 7. 非功能需求
- 安全：{鉴权/权限/数据写入边界/输入校验与速率限制}
- 性能：{…}　隐私：{…}　可访问性：{…}
- 可观测性：{日志/错误追踪（Sentry 类）/关键告警——上线前就位，不是出事后补}
- 数据安全网：{备份策略/迁移回滚预案——动生产数据前先有后悔药}

## 8. 验收标准（三轨制）
> 功能跑通但体验轨/旅程轨未达标，不算完成。

- **① 功能轨**：每个核心功能"算完成"的可验证标准（对应 PLAN 任务的完成定义）。**涉敏功能（鉴权/支付/PII/输入入库）必须含安全反例**（越权返回 403、注入样例被拒、超限被限流）；**后端质量属性（延迟/幂等/失败降级）也并入本轨**，别让验收只剩用户可见项。
- **② 体验轨（单页）**：交互四态齐全、响应式断点、可访问性、对照 DESIGN.md 一致。
- **③ 旅程轨（跨页）**：整条旅程连贯、无断头路、每步有明确下一步、结束态有结果反馈。
  （每个里程碑收口时按手册阶段 3.5 走查验收。）

## 9. 测试策略
- 单测覆盖：{哪些层/关键路径}
- 手动验收清单：覆盖交互四态与移动端断点，不只 happy path；每里程碑末尾走一遍完整旅程。

## 10. 架构蓝图
```mermaid
sequenceDiagram
  {关键业务/数据流}
```
- 技术选型与风险：{…}

## 11. 里程碑细节（第二层 · 开工前 JIT 补齐）
> 每个里程碑开工前，在此补该里程碑的字段级规格：完整契约细节（指向 shared 文件）、页面细化、边界情况。
> 只写即将开工的里程碑；已完成里程碑的细节若被后续修订取代，直接更新（历史进 CHANGELOG）。

### M{x} · {里程碑名}
- {字段级规格 / 契约细节指针 / 边界情况}
